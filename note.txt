Controllers => 控制輸入輸出(控制程式執行的流程)，但不決定輸出的內容
Controllers如果要取資料，會叫model取資料給Controllers，Controllers把資料丟給view

三個口訣:
1.controllers->要輕
2.model->要肥
3.view->要夠笨

選model右鍵新增項目->加入ADO.NET實體資料模型->Fabrics
->預設第一個
->選擇資料連接
->打勾所有table都加入，預設值加入

T-SQL string format @p0 @p1 @p2... 